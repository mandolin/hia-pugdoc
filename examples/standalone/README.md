# PugDoc Standalone Example

Runs the PugDoc runner against `src/pages/card.pug` and emits generated HTML, PugDoc extraction, HTMDoc bridge artifact, HIA document, doc-source-map manifest and a producer result.

```sh
pugdoc --config pugdoc.config.json
```
