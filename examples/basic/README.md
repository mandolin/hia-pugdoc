# Basic Example

The first runnable example is the generated-source fixture under:

```text
fixtures/doc-source-map/pug-html
```

Run:

```sh
npm install
npm run build:fixtures
npm run check:fixtures
```

The output demonstrates Pug source comments controlling generated HTML documentation and producing a `doc-source-map` manifest for later HIA tooling consumption.
