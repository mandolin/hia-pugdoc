# Pug HTMDoc Bridge

Bridges PugDoc extraction artifacts with HTMDoc semantics.

The P1 bridge converts PugDoc component and mixin symbols into HTMDoc extraction symbols while preserving Pug source metadata for source-linkage consumers.
